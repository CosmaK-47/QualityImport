# Copy this file to .env and paste the real token only into .env.
# The .env file is ignored by Git and must never be committed.
TELEGRAM_BOT_TOKEN=
INVENTORY_API_URL=http://localhost:3000/api/inventory/telegram
ORDERS_API_URL=http://localhost:3000/api/orders/telegram
# Generate a different random value of at least 24 characters. Use the same
# protected value in the website runtime as ORDER_SERVICE_SECRET.
ORDER_SERVICE_SECRET=
